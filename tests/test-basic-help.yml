name: Test gosec basic help
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Run gosec with help flag
        id: action
        uses: .
        with:
          args: "-h"
      - name: Assert help ran successfully
        run: |
          echo "PASS: gosec -h completed successfully"
